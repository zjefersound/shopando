import styled from 'styled-components';

export const Container = styled.div`
    background: var(--blue-light);
    height: 80px;
    display: flex;
    
    align-items: center;
    overflow: hidden;
`;